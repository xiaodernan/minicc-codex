# 改动影响与验收凭据实现计划

2026-09-18，用户要求在已有优化之外加入有技术含量、实用的功能。延续当前工作区，不重做已通过的主题与执行器，不引入外部基础设施。

| 项目 | 实现 | 验收 | 状态 |
|---|---|---|---|
| H1 改动影响分析 | Python AST 与 JS/TS 相对静态导入图；反向依赖遍历；显示实际依赖路径和受影响测试；增量缓存、扫描上限、解析失败和外部依赖如实披露 | 传递依赖、环、文件删除、src 布局、JS 相对路径、失效缓存；接口只读且不执行测试 | 进行中 |
| H2 Agent 与验证接线 | 独立 change_impact 只读工具；验证计划纳入传递关联测试，保留选择原因；Web 提供影响查看接口 | 真实工具注册/授权、HTTP 输入边界、定向测试选择；不默认全量 | 进行中 |
| H3 验收凭据 | 从持久任务快照导出有界脱敏记录：修改、检查、完成判定、未解决项、已知耗时/token；规范 JSON 摘要和离线校验 CLI | 完整性核对、篡改失败、字段缺失不伪造、取消/失败不粉饰、敏感文本脱敏；不宣称数字签名或代码正确性证明 | 进行中 |
| H4 前端解释与导出 | 检查器“影响”视图：改动→受影响文件→推荐检查及解释；任务详情增加凭据摘要/下载入口，明确局限与加载/错误状态 | 亮暗/手机、键盘和超长路径、竞态、下载、数据缺失、实际 HTTP 与 fake 任务流程 | 进行中 |
| H5 集成与文档 | 相关定向检查、构建、产品路径、接口示例及限制 | 可复现证据和截图，保留既有用户修改；无全量回归 | 待完成 |

边界：静态依赖图不是运行时覆盖率，动态导入/反射无法完全推断。凭据 SHA-256 只检测内容变化；没有签名时不证明作者身份，也不替代行为测试。影响页面和工具都不执行命令。
